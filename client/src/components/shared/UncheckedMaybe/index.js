import { QuestionCircle } from '@styled-icons/bootstrap/QuestionCircle';


const UncheckedMaybe = (concertId) => {
    return (
        <div>
            <QuestionCircle className='rsvp-maybe' />
        </div>
    )
}

export default UncheckedMaybe
